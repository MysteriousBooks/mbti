# Design Implementation Plan: Result 结果页 · 居中态 D 变体定稿

## Summary
- **Scope:** component（Result 组件居中态渲染）
- **Target:** `src/components/Result.tsx`、`src/lib/scoring.ts`、`src/index.css`
- **Winner variant:** D（候选名筛选按钮 + 黄底详情面板）
- **Key improvements:** x 格虚线弱化、长文拼接改为候选筛选器、特质/按钮间距加大

## Files Changed (已实施)
- [x] `src/lib/scoring.ts`
  - `ScoreResult` 增加可选 `candidates?: TypeCandidate[]`
  - `mergeTypeParts` 改为结构化：summary 变成引导语，候选完整数据进 candidates
  - `resolveType` 透传 candidates（`...(lookup.candidates ? {candidates} : {})`）
- [x] `src/components/Result.tsx`
  - `formatCodeCells` 增加 `isX` 标记
  - 居中态渲染候选筛选按钮（按钮 + aria-pressed）+ 黄底详情面板（`key={cur.code}` 触发切换动画）
  - 确定态渲染路径不变
- [x] `src/index.css`
  - `.f-code-cell-x`：虚线边框 + 灰调 + 小字号
  - `.f-x-hint`：`? = 两极倾向接近（居中）`
  - `.f-candidates` / `.f-cand-btn` / `.f-cand-panel*`：完整筛选器样式
  - `.f-traits` 间距 `gap 8→10`、`margin-top 16→24`
  - `.result-actions` 间距 `gap 10→12`

## Component API
- **ScoreResult.candidates?: TypeCandidate[]** — 居中态候选列表（≤1 个时不渲染筛选器）
- **TypeCandidate** = `{ code, name, summary, traits }`（traits 已截断为前 3 条）

## Required UI States
- **默认/确定态：** 无候选筛选器，单大名 + summary（现有路径）
- **居中态：** x 格显示 `?`，候选筛选按钮默认选中第 0 个，详情面板黄底展示 traits+summary
- **全 x：** `ALL_NEUTRAL` 走通用文案，candidates 为 undefined

## Accessibility Checklist
- [x] x 格仍参与 `role="img"` 的 aria-label（完整码含 x，连字符格式）
- [x] 候选按钮带 `aria-pressed`、44px 最小触控
- [x] 详情面板切换用 `key={cur.code}` 触发 React 重建（动画重置）
- [x] focus-visible 蓝描边沿用全局 `.btn:focus-visible` 规则
- [x] `prefers-reduced-motion` 全局规则覆盖（index.css 顶部）

## Testing
- [x] `scoring.test.ts`：居中态断言新的引导语 + candidates 结构
- [x] `Result.test.tsx`：新增居中态筛选按钮 + 详情面板测试（点选切换）
- [x] 全量 54 测试通过、`npm run build` 成功

## Design Lab Cleanup
- [x] `.claude-design/`、`src/design_lab_*` 已全部删除
- [x] `main.tsx` 恢复为仅渲染 App
- [x] `scoring.ts.patch.txt`、`design_lab_fixtures.json` 临时文件删除

## Design Memory Update
详见 `DESIGN_MEMORY.md`（追加居中态组件与 x 格样式）

---

*Generated by Design Variations plugin*
